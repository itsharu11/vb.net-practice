Module Module1

    Sub Main()
        Dim a As String
        Dim n As Integer
        a = Console.ReadLine()
        n = Len(a)
        Console.WriteLine("Length = " & n)
        Console.ReadKey()
    End Sub

End Module
